const positiveInt=v=>Number.isSafeInteger(v)&&v>0?v:null;
export function sourceCharacteristics(block,rpc=false){return {characteristicsVersion:1,txCount:positiveInt(rpc?block.nTx??block.tx?.length:block.tx_count),sizeBytes:positiveInt(block.size),weight:positiveInt(block.weight)};}
export function characterize(blocks){const measure=(key)=>{const values=blocks.map(b=>b[key]).filter(v=>Number.isSafeInteger(v)&&v>0);return {observations:values.length,average:values.length?values.reduce((a,b)=>a+b,0)/values.length:null};};const tx=measure('txCount'),coinbaseOnly=blocks.filter(b=>b.txCount===1).length;const recipients=blocks.filter(b=>Array.isArray(b.outputs)).map(b=>new Set(b.outputs.filter(o=>o.sats>0).map(o=>o.address)).size);return {blocks:blocks.length,transactions:tx,sizeBytes:measure('sizeBytes'),weight:measure('weight'),coinbaseOnly:{observations:tx.observations,count:tx.observations?coinbaseOnly:null,share:tx.observations?coinbaseOnly/tx.observations:null},recipients:{observations:recipients.length,average:recipients.length?recipients.reduce((a,b)=>a+b,0)/recipients.length:null,single:recipients.length?recipients.filter(n=>n===1).length:null,multiple:recipients.length?recipients.filter(n=>n>1).length:null}};}

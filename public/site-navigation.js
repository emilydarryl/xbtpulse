const nav=document.querySelector('.site-navigation'),button=nav?.querySelector('.site-menu-toggle'),tools=nav?.querySelector('.site-tools');
if(nav){nav.classList.add('nav-enhanced');const close=()=>{nav.classList.remove('menu-open');button.setAttribute('aria-expanded','false');tools.open=false;};button.addEventListener('click',()=>{const open=nav.classList.toggle('menu-open');button.setAttribute('aria-expanded',String(open));});document.addEventListener('click',e=>{if(!nav.contains(e.target))close();});document.addEventListener('keydown',e=>{if(e.key==='Escape'){if(tools.open){tools.open=false;tools.querySelector('summary').focus();}else if(nav.classList.contains('menu-open')){close();button.focus();}}});}
